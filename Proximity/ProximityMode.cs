namespace Proximity
{
    public enum ProximityMode
    {
        Edge, // Measure from the pointer to the edge of the element
        Center // Measure from the pointer to the center point of the element
    }
}
